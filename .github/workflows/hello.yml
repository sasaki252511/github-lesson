name: Schedule
on: push
jobs:
  print:
    runs-on: windows-latest
    env:
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - run: echo "${PASSWORD}"
      - run: echo "${PASSWORD:0:1} test ${PASSWORD#?}"
