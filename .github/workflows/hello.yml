name: Schedule
on: push
jobs:
  print:
    runs-on: windows-latest
    env:
      PASSWORD: SecretPassword!
    steps:
      - run: echo "${PASSWORD}"
      - run: echo "${PASSWORD:0:1} ${PASSWORD#?}"
