name: Status check functions
on: push
jobs:
  skip-or-run:
    runs-on: windows-latest
    steps:
      - run: echo "${CONTEXT}"
        env:
          CONTEXT: ${{toJSON(github)}}
        if: ${{github.actor == 'sasaki252511'}}
      - run: echo "Hello"
